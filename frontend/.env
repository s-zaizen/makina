# Build-time defaults for SvelteKit's `$env/static/public`.
# Override per environment by exporting these in the shell before
# `npm run build` (Cloud Run / Pages deploy pipelines do this).

PUBLIC_API_URL=
PUBLIC_MAKINA_PUBLIC_MODE=
