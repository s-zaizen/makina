{
	"name": "makina-api-proxy",
	"version": "0.1.0",
	"private": true,
	"description": "Cloudflare Worker reverse proxy: api.makina.sh → Cloud Run",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^3.95.0"
	}
}
